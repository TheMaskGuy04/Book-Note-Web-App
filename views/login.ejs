<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Note - Sign In</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    <link rel="stylesheet" href="styles/login.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Rubik+Doodle+Shadow&display=swap" rel="stylesheet">
</head>
<body>

    <h1>Welcome to Book Note Web App!</h1>
    <div class="container">
        <% if (logIn == 1) { %>
            <h3>Log In Here!</h3>
            <form action="/logIn" method="POST">
        <% } %>

        <% if (register == 1) { %>
            <h3>Register Here!</h3>
            <form action="/register" method="POST">
        <% } %>

            <div class="form-floating">
                <input type="email" class="form-control" id="floatingInput" name="email" placeholder="name@example.com" fdprocessedid="jfnah" required>
                <label for="floatingInput">Email    address</label>
            </div>
            <div class="form-floating">
                <input type="password" class="form-control" id="floatingPassword" placeholder="Password" name="password" fdprocessedid="au50mc" required>
                <label for="floatingPassword">Password</label>
            </div>


            <% if (logIn == 1) { %>
                <button class="logIn btn btn-primary w-25 py-2 mb-4" type="submit" fdprocessedid="okj35">Log In</button>

                <% if (locals.error) { %>
                    <p style="color: rgb(255, 2, 2);"><%= error %></p>
                <% } %>

                <p>Not Registered ? <a href="/registerPage">Register here</a></p>
            <% } %>
            
            <% if (register == 1) { %>

                <button class="logIn btn btn-primary w-25 py-2 mb-4" type="submit" fdprocessedid="okj35">Register</button>

                <% if (locals.error) { %>
                    <p style="color: rgb(255, 2, 2);"><%= error %></p>
                <% } %>

                <p>Already Registered ? <a href="/logInPage">Login here</a></p>
            <% } %>
            
            <p class="mt-2 mb-3 text-body-secondary">©2024</p>
        </form>
    </div>    

</body>
</html>